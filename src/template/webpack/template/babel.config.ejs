module.exports = {
    presets: [
        [
            '@babel/preset-env',
            {
                useBuiltIns: 'usage',
                corejs: { version: 3 },
                targets: "> 0.25%, not dead"
            }
        ]
    ],
    plugins: ["@babel/plugin-transform-runtime"]
}